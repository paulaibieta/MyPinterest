<%= form_for @pin do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title, placeholder: @pin.title %>
    <%= f.label :description %>
    <%= f.text_field :description, placeholder: @pin.title %>
    <%= f.submit 'submit corrections' %>
  <% end %>